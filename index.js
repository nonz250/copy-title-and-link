javascript: (function () { const text = document.querySelector('title').textContent + "\n" + window.location.href; if (navigator.clipboard) { navigator.clipboard.writeText(text); return; } const textarea = document.createElement('textarea'); textarea.textContent = text; textarea.style.height = '0px'; textarea.style.width = '0px'; const body = document.body; body.appendChild(textarea); textarea.select(); document.execCommand('copy'); body.removeChild(textarea); })();
